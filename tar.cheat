% tar

# Extract tar
tar -xfv <tar_file>

# Extract tar.gz
tar -xzfv <tar_gz_file>

$tar_file: find . -maxdepth 1 -name "*.tar" -o -name "*.tar" 
$tar_gz__file: find . -maxdepth 1 -name "*.tar.gz" -o -name "*.tgz" 
