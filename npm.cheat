% npm

# Install a package
npm -g install <package>

# Update all installed packages
npm update -g

# Update npm
npm install -g npm

# Address issues
npm audit fix

# Remove cache
npm cache clean --force 
