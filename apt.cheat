% apt

# Install a package
<sudo_cmd> apt install <package>

# Sync packages data from repositories
apt update

# Install updated packages
apt upgrade

# See upgradable packages
apt list --upgradable 

# Uninstall a package
apt remove <package>

# Delete unnecessary packages
apt autoremove
 
#

$ sudo_cmd: [ "$(id -u)" -ne 0 ] && echo sudo || echo ""
