% nmap

# See devices connected to network
sudo nmap -sn <your_ip>/<CIDR>

# check if port is open
nmap -p <port> -Pn <ip>

# See open ports of device
sudo nmap -p <initial_port>-<last_port> -Pn <ip>

#
