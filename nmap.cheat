% nmap

# See devices connected to network
sudo nmap -sn 192.168.1.0/24

# check if port is open
nmap -p <port> -Pn <ip>

# Scan fast the most common ports
sudo nmap -F <ip>

# Scan all ports
sudo nmap -p- <ip>


