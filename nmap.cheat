% nmap

# See devices connected to network
sudo nmap -sn <your_ip>/<CIDR>

# check if port is open
nmap -p <port> -Pn <ip>

# Scan fast the most common ports
sudo nmap -F <ip>

# Scan all ports
sudo nmap -p- <ip>


