% shutdown

# Turn off now
sudo shutdown now

# Turn off at a time
sudo shutdown <hour>:<minute>

# Turn off after given minutes
shutdown -h <minutes>

# Reboot
sudo shutdown -r

# Reboot at a time
sudo shutdown -r <hour>:<minute>
