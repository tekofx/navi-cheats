% shutdown

# Turn off now
shutdown now

# Turn off at a time
shutdown <hour>:<minute>

# Turn off after given minutes
shutdown -h <minutes>

# Reboot
shutdown -r

# Reboot at a time
shutdown -r <hour>:<minute>
